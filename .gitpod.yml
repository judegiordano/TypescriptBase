# Commands to start on workspace startup
tasks:
  - name: Environment 1
    before: yarn install
    init: yarn build
    command: yarn build

# Ports to expose on workspace startup
ports:
  - port: 3000
    onOpen: open-preview
